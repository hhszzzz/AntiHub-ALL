# 浏览器侧请求后端地址（= 后端对外地址；根目录 compose 默认 http://localhost:8000）
NEXT_PUBLIC_API_URL=http://localhost:8000

# Next.js 服务器侧请求后端地址（Docker Compose 内可用 http://antihub-backend:8000；留空则自动推断）
BACKEND_INTERNAL_URL=

# Cookie 协议：HTTPS 或 HTTP（用 IP 直连 HTTP 时改成 HTTP）
COOKIE_HTTP=HTTPS
